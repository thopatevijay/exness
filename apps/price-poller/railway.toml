[build]
builder = "RAILPACK"
buildCommand = "pnpm install --frozen-lockfile && pnpm exec turbo run build --filter=@exness/price-poller"
watchPatterns = ["apps/price-poller/**", "packages/**", "pnpm-lock.yaml", "package.json"]

[deploy]
startCommand = "pnpm --filter @exness/price-poller start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
